// Teste placeholder. O app real abre conexão MQTT no initState, então um
// widget test completo exigiria mockar o broker — fica pra depois.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
